<!DOCTYPE html>
<html>
  <head>
    <title>NoMore</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="main-header" data-controller="header">
      <div class="header-logo">
        <%= link_to "No More", root_path %>
      </div>
      
      <!-- ハンバーガーメニューボタン（モバイル用） -->
      <button class="hamburger-menu" data-header-target="hamburger" data-action="click->header#toggle" aria-label="メニューを開く">
        <span></span>
        <span></span>
        <span></span>
      </button>
      
      <nav class="header-nav" data-header-target="nav">
        <% if user_signed_in? %>
          <%= link_to "ホーム", home_path, class: "header-btn" %>
          <%= link_to "投稿作成", new_post_path, class: "header-btn" %>
          <%= link_to "ユーザー一覧", users_path, class: "header-btn" %>
          <%= link_to user_path(current_user), class: "header-btn user-profile-btn", style: "display: flex; align-items: center; gap: 8px; text-decoration: none;" do %>
            <% if current_user.has_profile_image? %>
              <%= image_tag current_user.profile_image, style: "width: 30px; height: 30px; border-radius: 50%; object-fit: cover;" %>
            <% else %>
              <div style="width: 30px; height: 30px; border-radius: 50%; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center; color: white; font-weight: bold; font-size: 12px;">
                <%= current_user.name.first.upcase if current_user.name.present? %>
              </div>
            <% end %>
            <span class="user-name-text">マイページ</span>
          <% end %>
          <%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete }, class: 'header-btn' %>
          <%= form_with url: search_path, method: :get, class: "header-search-form", local: true do |f| %>
            <%= f.text_field :q, placeholder: "投稿・ユーザー検索...", class: "header-search-input", value: params[:q] %>
            <%= f.submit "検索", class: "header-search-btn" %>
          <% end %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "header-btn" %>
        <% end %>
      </nav>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
